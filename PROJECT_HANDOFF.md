# DIVE 상담 리포트 — LLM 인수인계 문서

> 최종 정리일: 2026-09-22 (Asia/Seoul)  
> 다음 담당: Claude 또는 후속 LLM  
> 목적: 지금까지 구현·수정·배포한 내용을 다시 탐색하지 않고 바로 이어서 작업할 수 있도록 정리

> 검수판: 2.0 — GPT-6 소스 대조 검수, 2026-09-22. 이번 검수는 문서 수정과 코드 읽기이며 새 라이브 QA·빌드·배포를 수행한 것은 아니다.

## 먼저 읽을 핵심 정정

이 서비스는 CRUD와 상담 업무 UI가 구현된 상태지만 AI/STT 실처리, 외부 결과 공유, 역할별 접근 제어까지 완성된 서비스로 인계하면 안 된다.

- RTP `analyze()`는 고정 `extractedRtpResult`를 복제한다. 실제 PDF 분석 호출이 아니다.
- `ResultRegistration`의 `sttTranscript`는 코드 내 예시 배열이다. 음성 재생 팝업 확인을 STT 성공으로 해석하지 말 것.
- `shareReport()`는 학생명 기반 `/?report=...`를 공유한다. 확인한 앱에는 이 파라미터로 결과를 조회하는 전용 처리 경로가 없다. 외부 독립 브라우저에서 해당 결과가 열리는지부터 구현·검증해야 한다.
- 녹음의 중간 캡처 포맷은 WebM/MP4일 수 있지만 저장은 `convertRecordingToWav()`로 PCM 16-bit WAV 변환 후 업로드한다.
- 음성 입력 UI는 `audio/*`도 허용하지만 서버는 MP3의 `audio/mpeg`를 허용하지 않는다.
- 전송 완료 시각은 클라이언트에 생성되지만 현재 확인한 상담 API와 DB 스키마에서 저장·반환하지 않는다.
- 역할별 권한 매트릭스는 기획안이다. 실제 API 권한 검사와 호스팅 접근 정책을 별도로 확인해야 한다.
- 기획 산출물: [DIVE_PRODUCT_SPEC.md](DIVE_PRODUCT_SPEC.md). 제안과 현행을 구분해서 읽을 것.

## 1. 프로젝트 현황

- 서비스명: **DIVE 상담 리포트**
- 용도: 학생 상담 예약, 자료 등록, RTP 결과 분석, 상담 결과 작성, 학부모 공유 및 등록 여부 추적
- 로컬 프로젝트: `C:\Users\black\OneDrive\문서\ChatGPT\dive project`
- GitHub: `git@github.com:blackol13/dive-consultation-report.git`
- 현재 작업 브랜치: `codex/consultation-workflow`
- 현재 로컬 HEAD: `566f21d docs: add project handoff guide` (앞선 작업에서 GitHub main 푸시 성공 확인; 이번에는 원격 재조회하지 않음)
- 최신 애플리케이션 변경 커밋: `19f018a feat: refine responsive consultation interface`
- 라이브 URL: <https://dive-consultation-report.jaehwa0717.chatgpt.site/>
- 마지막으로 성공 확인한 배포: Sites 버전 28, 커밋 전체 SHA `19f018aef84c99b862dbf30c3e5bd410cb39f2c1` (2026-09-01; 현재 라이브 버전 재조회는 하지 않음)
- Sites 프로젝트 ID: `.openai/hosting.json`에 저장되어 있음
- 영속 저장소:
  - D1 바인딩: `DB`
  - R2 바인딩: `FILES`

보안상 배포용 임시 토큰이나 쓰기 자격 증명은 이 문서에 기록하지 않는다. 다음 배포 때 Sites 도구를 통해 새 자격 증명을 발급받아야 한다.

## 2. 기술 스택과 실행 방법

- React 19
- TypeScript
- Vinext / Vite
- Cloudflare Workers 호환 빌드
- Drizzle ORM + D1
- R2 파일 저장
- Node.js 요구 버전: `>=22.13.0`

package.json의 dev/build/start는 POSIX 환경변수 문법을 사용한다. 아래 npm 명령은 POSIX 셸 기준이며 Windows PowerShell에서는 다음 런타임 예시처럼 환경변수를 설정한 뒤 `npx vinext dev/build/start`를 실행한다.

주요 명령:

```powershell
# 개발 서버
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 + 렌더링 테스트
npm test

# 정적 검사
npm run lint

# DB 마이그레이션 생성
npm run db:generate
```

이 컴퓨터에는 프로젝트 내부 Node 런타임이 있다. 시스템 Node가 맞지 않을 경우:

```powershell
$env:PATH=(Resolve-Path '.\.runtime\node-v22.23.2-win-x64').Path+';'+$env:PATH
$env:WRANGLER_LOG_PATH='.wrangler/wrangler-build.log'
npx vinext build
```

## 3. 핵심 파일 구조

| 파일/폴더 | 역할 |
|---|---|
| `app/page.tsx` | 전체 SPA 화면, 상태, 상담 워크플로우, 파일 관리 UI의 대부분 |
| `app/globals.css` | 데스크톱·태블릿·모바일 적응형 스타일 전체 |
| `app/layout.tsx` | 앱 레이아웃과 메타데이터 |
| `app/chatgpt-auth.ts` | 호스팅 인증 관련 코드 |
| `worker/index.ts` | `/api/consultations`, `/api/files`, `/api/settings` 등 Worker API |
| `db/schema.ts` | D1 스키마 |
| `db/runtime.ts`, `db/index.ts` | DB 연결 및 데이터 접근 |
| `db/migrations/` | 운영 스키마, 자료 등록 플래그, 설정 마이그레이션 |
| `tests/rendered-html.test.mjs` | 렌더링 결과 테스트 |
| `.openai/hosting.json` | Sites 프로젝트 ID와 D1/R2 논리 바인딩 |
| `public/dive-logo.png` | 브랜드 로고 |

`app/page.tsx`가 매우 큰 단일 파일이다. 기능 변경 시 상태와 뷰가 서로 강하게 연결되어 있으므로 함부로 일부만 떼어내지 말 것. 장기적으로는 화면별 컴포넌트와 훅으로 분리하는 리팩터링이 필요하지만, 현재는 동작 보존이 우선이다.

## 4. 현재 사용자 흐름

### STEP 1 — 학생 정보

- 학생 이름
- 학교 구분: 초등학교/중학교
- 학교명
- 학년: 학교 구분 선택 후 활성화
- 학부모 휴대번호
- 상담 예약 날짜·시간
- 담당자 메모
- 필수 정보가 채워져야 `상담 예약` 버튼 활성화
- 최종 등록 전 확인 팝업 표시
- 등록 완료 시 상담 기록의 `상담 대기` 필터로 이동

### STEP 2 — 자료 등록

- RTP PDF 업로드 또는 기존 등록 파일 선택
- 상담 음성 업로드 또는 기존 등록 파일 선택
- 각 자료는 `미등록` 선택 가능
- 기존 등록 파일은 `/api/files`에서 실시간 로드
- 샘플 파일은 워크플로우 선택 목록에서 제외
- 학생 정보 카드 하단에 담당자 메모 표시
- 학생 정보 수정 모달 제공
- `상담 기록 목록` 복귀 버튼 제공
- 자료 등록 완료 후 STEP 3 이동

### STEP 3 — 결과 작성

- RTP 결과 점수/분석 결과 표시
- 레벨 테스트 원장 리뷰
- STT 상담 내용 요약
- 상담 대화 내역 확인 모달
- 학생 레벨과 DIVE 반 배정
- 원장 코멘트
- 학생 정보와 담당자 메모 표시
- 학생 정보 수정 모달 제공
- `상담 기록 목록` 복귀 버튼 제공
- 작성 완료 확인 시 상태를 `작성 완료`로 변경하고 같은 상태 필터의 상담 기록으로 복귀. 담당자가 완료 기록을 다시 열어 STEP 4 진입

### STEP 4 — 결과 공유

- 링크 공유
- PDF 열기/인쇄
- 상담 후 팔로업:
  - 등록 여부: 미확인/등록 완료/미등록
  - 등록일
  - 기타 의견
- 상단에 `상담 기록 목록` 복귀 버튼
- 하단의 기존 `상담 기록으로 →` 버튼은 삭제됨
- `전송 완료` 버튼은 하단 워크플로우 액션 위치로 이동됨
- 전송 완료 클릭 시 확인 팝업
- 확인 시 상태 변경 저장을 요청하고 해당 필터의 목록으로 이동. 서버 저장 성공 전에 화면이 먼저 바뀌는 구조이므로 실패·재접속 검증 필요

## 5. 화면별 구현 내용

### 홈

- 오늘 날짜 표시
- 상담 음성 바로 녹음
- 신규 상담 등록
- 첨부파일 관리
- 오늘 상담 예정 수치
- 상담 결과 작성 대기 수치
- `오늘 상담 예정` 카드는 파란색 2px 테두리와 파란 수치로 강조
- `전체 상담 대기 보기 →`는 모바일에서 카드 우측 상단 배치

### 상담 기록

- 상태 필터:
  - 상담 대기
  - 상담 작성 중
  - 결과 작성 완료
  - 결과 전송 완료
- 검색: 학생명 또는 학교명
- 워크플로우에서 목록으로 돌아오면 기존 필터 유지
- 일정 태그 정렬 우선순위:
  1. Today
  2. Upcoming
  3. Past
  4. 같은 그룹에서는 최신 날짜·시간 우선
- Today/Upcoming/Past 태그는 `상담 대기`, `상담 작성 중`에서만 표시
- `작성 완료`, `결과 전송 완료`에서는 일정 태그 숨김
- `결과 전송 완료`에서만 등록 여부 표시
- 모바일 카드에서는 학생 이니셜 원형 아이콘 숨김
- 모바일에서 학생명은 카드 상단 왼쪽 정렬
- 모바일 등록 여부는 학년과 같은 오른쪽 열에 정렬
- 태블릿/데스크톱 테이블은 컨테이너 폭에 맞춰 컬럼 리사이즈
- 테이블에 가로 스크롤이 생기지 않도록 고정 최소 너비 제거 및 말줄임 처리

### 첨부파일 관리

- 탭:
  - 레벨 테스트 PDF
  - 상담 음성 WAV/MP4
- 모바일 하단 내비게이션에서 첨부파일 선택 시 녹음 팝업이 자동으로 열리지 않음
- PDF 등록 버튼과 녹음 파일 등록 버튼은 모바일 카드 우측 상단
- 바로 녹음은 별도 버튼
- 브라우저 녹음 결과는 파일명 확인 후 저장
- 중간 녹음은 브라우저 지원 포맷(WebM/MP4)을 사용하고 저장 시 `convertRecordingToWav()`가 WAV로 변환. 확장자만 바꾸는 구현이 아님
- 서버 음성 허용 MIME은 `audio/wav`, `audio/x-wav`, `video/mp4`, `audio/mp4`, `audio/webm`. MP3(`audio/mpeg`)는 현재 거부됨. UI의 `audio/*` 안내와 불일치
- 파일 행 모바일 배치:
  - 삭제: 행 우측 상단
  - 미리보기/재생: 파일명 시작선에 맞춘 좌측 하단
  - 다운로드: 미리보기 오른쪽
- 아이콘:
  - PDF 미리보기: 눈 아이콘
  - 음성 미리듣기: 재생 아이콘
  - 다운로드: 내려받기 SVG 아이콘
- 샘플 파일은 다운로드와 삭제 비활성화
- 업로드된 실제 파일만 STEP 2 기존 파일 선택 목록에 표시

### 학생 정보 수정 모달

- `변경사항 반영` 클릭 시 “수정된 내용을 반영하시겠습니까?” 확인 팝업
- 확인: 정보 저장 및 모달 종료
- 취소: 확인 팝업 닫기
- 날짜·시간·메모 포함 수정 가능

### 반응형/적응형 웹

- 데스크톱: 좌측 사이드바
- 모바일: 하단 고정 내비게이션
- 모바일 STEP 네비게이터는 4단계 압축 표시
- STEP 3 학생 정보는 모바일에서 2×2 그리드
- STEP 4 목록 복귀 버튼은 네비게이터 아래 우측의 정상 크기 버튼
- 모바일 카드/파일 목록/모달/폼을 한 열로 재배치
- 최소 지원 폭은 320px
- QA한 크기:
  - 396×912 모바일
  - 1031×912 태블릿/소형 데스크톱
  - 기본 인앱 브라우저 약 910×698
- 위 크기에서 문서 및 상담 기록 테이블 가로 스크롤 없음 확인

## 6. 데이터와 API 동작

### 상담 데이터

- `GET /api/consultations`: 상담 목록 로드
- `POST /api/consultations`: 신규 상담 생성
- `PUT /api/consultations`: 상담 자동 저장 및 상태 업데이트
- `DELETE /api/consultations?id=...`: 상담 기록 삭제
- 상담 편집 중 주요 상태는 350ms 디바운스로 자동 저장

상담 상태 값:

```text
상담 대기
상담 작성 중
작성 완료
결과 전송 완료
```

### 파일

- `GET /api/files`: 파일 목록
- `POST /api/files`: R2 업로드
- `GET /api/files?id=...`: 다운로드/미리보기
- `DELETE /api/files?id=...`: 삭제
- 카테고리:
  - `rtp`
  - `consultation`

### 설정

- `GET /api/settings`: DIVE 반 시간표 로드
- `PUT /api/settings`: DIVE 반 시간표 저장

## 7. 최근 커밋 흐름

```text
19f018a feat: refine responsive consultation interface
68293f1 feat: adapt layouts and sync registered files
9d310b7 feat: refine consultation workflow and recording
af7723e feat: refine consultation workflow and reporting
b65903d feat: refine consultation history views
6485e5f feat: classify waiting consultations by schedule
70b4356 feat: default consultation history to waiting
8db36bf copy: clarify dashboard consultation metrics
```

`19f018a`에는 최종 반응형 조정, 파일 버튼 배치, 홈 강조 카드, 상담 기록 컬럼 리사이즈 등이 포함되어 GitHub `main`과 라이브 버전 28에 배포되어 있다.

## 8. 2026-09-22 라이브 기능 QA 결과

실제 라이브 사이트를 인앱 브라우저로 점검했다. 운영 데이터 변경을 피하기 위해 최종 저장/삭제/전송 확인은 실행하지 않았다.

### 관찰한 범위 — 전체 기능 통과와 구분

아래는 앞선 브라우저 검사의 관찰 기록이다. 미리듣기는 팝업 진입까지만 확인했으며 실제 오디오 로드·재생 완료는 확인하지 않았다. 필터는 확인했으나 Today/Upcoming 데이터와 전송 완료 데이터가 없어 모든 정렬·상태 조합을 검증하지 못했다. 이전 답변의 ‘통과 18개’는 테스트 케이스 집계 근거가 없으므로 정식 통과 건수로 인용하지 말 것.

- 홈 화면 주요 메뉴 이동
- 신규 상담 등록 화면 진입
- 필수 입력 전 상담 예약 버튼 비활성화
- 학교 구분 선택 후 학년 활성화
- 상담 기록 상태별 필터
- 학생명/학교명 검색 및 결과 없음 상태
- 작성 완료/결과 전송 완료에서 일정 태그 숨김
- 기존 상담 재진입
- STEP 2·3·4 상태 표시
- 목록 복귀 후 필터 유지
- 학생 정보 수정 모달
- 수정 반영 확인 팝업과 취소
- 전송 완료 확인 팝업과 취소
- PDF/상담 음성 탭 전환
- 첨부파일 탭 진입 시 녹음 팝업 자동 미표시
- 실제 등록 음성 파일 목록 로드
- 바로 녹음 준비 팝업과 취소
- 음성 미리듣기 팝업 진입
- 모바일 학생 이니셜 숨김
- 모바일 파일 액션 정렬
- 모바일/태블릿 가로 스크롤 없음

### 발견된 결함 1 — React hydration 오류

라이브 최초 로딩 시 콘솔에 아래 오류가 1회 발생했다.

```text
Minified React error #418
```

화면 동작은 가능했지만 최초 렌더 불일치 오류가 기록되었다. 다음 날짜 코드는 조사 대상 중 하나이며 원인을 재현·확정한 상태는 아니다:

```tsx
const todayLabel = new Intl.DateTimeFormat("ko-KR", {
  year: "numeric",
  month: "long",
  day: "numeric",
  weekday: "long",
}).format(new Date());
```

`Dashboard` 렌더링 중 `new Date()`를 직접 사용하고 있으며 서버/클라이언트 시간대 또는 렌더 시점이 다르면 hydration mismatch가 발생할 수 있다. `today()` 함수도 로컬 시간에 의존한다.

권장 해결:

1. 서버와 클라이언트가 같은 기준 시점·시간대의 날짜 문자열을 사용하도록 한다. 시간대 통일만으로 자정 경계나 포맷 차이까지 해결되지는 않으므로 SSR과 첫 클라이언트 렌더 값의 일치를 검증한다.
2. 또는 마운트 이후 날짜를 설정하고 최초 SSR에는 안정적인 placeholder를 사용한다.
3. 수정 후 프로덕션 빌드 및 라이브 콘솔 오류 재검증.

### 개선 필요 1 — 빈 결과 작성 완료 가능

STEP 3의 세 textarea가 모두 비어 있어도 `작성 완료 →` 버튼이 활성화된다. 빈 결과지를 실수로 완료 처리할 수 있다.

권장 해결:

- 필수 필드 정의 후 `disabled` 조건 추가
- 또는 완료 클릭 시 누락 항목을 설명하는 확인/검증 메시지 표시
- RTP 미등록과 음성 미등록이 허용되므로 필수 조건을 자료 존재 여부가 아닌 결과 텍스트/배정 값 중심으로 설계할 것

### 데이터 부족으로 미검증

- QA 당시 `결과 전송 완료` 기록이 0건이어서 상담 기록 카드의 등록 여부 표시를 실제 운영 데이터로 검증하지 못했다.
- 실제 마이크 권한 허용과 녹음 저장은 실행하지 않았다.
- 실제 파일 업로드·다운로드·삭제는 운영 데이터 보호를 위해 실행하지 않았다.
- 상담 신규 생성, 작성 완료 확정, 전송 완료 확정도 실행하지 않았다.

## 9. 다음 담당자가 우선 처리할 작업

우선순위 순서(검수판):

0. 외부 공개·실운영 전에 API 접근 권한, 고정 AI/STT 출력, 독립 결과 공유 경로를 먼저 점검한다. 상세 결함은 아래 14절 참고.

1. **React #418 hydration 오류 수정**
2. **STEP 3 완료 조건 정의 및 검증 추가**
3. 로컬에서 `npm test`, `npm run lint`, 프로덕션 빌드 수행
4. 테스트 전용 상담을 생성해 전체 E2E 수행:
   - 상담 생성
   - PDF/음성 업로드
   - 자료 선택
   - 결과 작성
   - 작성 완료
   - 전송 완료
   - 등록 여부 업데이트
   - 상담 기록 정렬/필터 검증
   - 테스트 데이터 삭제
5. 실제 모바일 Safari/Chrome에서 마이크 녹음 및 파일 포맷 확인
6. 단일 `app/page.tsx`를 기능 단위로 점진적 분리

## 10. 다음 작업 시 주의사항

- 기존 사용자 데이터가 라이브 D1/R2에 있으므로 QA 중 임의 삭제하지 말 것.
- 샘플 파일과 실제 업로드 파일의 동작이 다르다. 샘플은 삭제/다운로드/워크플로우 선택이 제한된다.
- 상담 기록에서 카드 클릭은 기존 상담을 재개한다. 테스트 중 자동 저장이 발생할 수 있다.
- 학생 정보 수정 모달에서 확인까지 누르면 실제 데이터가 변경된다.
- `작성 완료`, `전송 완료` 확인은 상태와 필터를 변경한다.
- 브라우저 마이크 권한은 사용자의 명시적 동의 없이 허용하지 말 것.
- `globals.css` 후반부에 반응형 override가 많이 누적되어 있다. 기존 규칙보다 파일 뒤쪽 규칙이 우선하므로 변경 시 cascade를 확인할 것.
- 모바일 기준 breakpoint가 `560px`, `600px`, `700px`, `900px` 등으로 나뉘어 있다.
- 테이블 가로 스크롤 제거 규칙은 `min-width:561px` 이상 상담 기록 컬럼에 별도로 적용되어 있다.
- 작업 전 `git status --short`로 사용자 변경과 임시 파일을 구분할 것.

## 11. 현재 Git 작업 트리 주의

인수인계 문서 생성 직전, 추적되지 않은 파일은 다음과 같았다.

```text
.preview-err.log
.preview-out.log
site-package-19f018a.tar.gz
site-package-68293f1.tar.gz
site-package-9d310b7.tar.gz
site-package-af7723e.tar.gz
site-package-b65903d.tar.gz
```

이 파일들은 개발 서버 로그와 이전 Sites 배포 패키지다. 소스 커밋에 포함하지 않았다. 삭제가 필요하면 프로젝트 루트 내부의 정확한 경로인지 확인한 뒤 처리할 것. 이 문서는 `566f21d`로 이미 추적 중이며 현재 검수 수정분은 미커밋 상태다. 기획서 `DIVE_PRODUCT_SPEC.md`는 현재 새 파일이다.

## 12. 배포 절차

이 프로젝트에는 `.openai/hosting.json`이 있으므로 **Sites 빌드/호스팅 절차**를 따라야 한다.

일반 순서:

1. 변경사항 검증
2. `npm run build`
3. 소스 커밋
4. GitHub `main` 푸시
5. Sites 소스 저장소용 쓰기 자격 증명 발급
6. 같은 커밋을 Sites 저장소 `main`에 푸시
7. Sites 호스팅 스킬의 `package-site.sh`로 `dist` 배포 아카이브 생성
8. 정확한 커밋 SHA로 Sites 버전 저장
9. 사이트 접근 정책 확인
10. 저장된 버전을 프로덕션 배포
11. 배포 상태가 `succeeded`가 될 때까지 확인
12. 라이브 URL 재확인

배포 아카이브는 소스 트리가 아니라 성공한 빌드의 `dist`를 포함해야 한다. 기존 아카이브를 재사용하지 말고 현재 커밋과 일치하는 새 아카이브를 만든다.

## 13. 완료 기준

후속 변경은 아래를 모두 충족해야 완료로 간주한다.

- 요청 기능 구현
- 데스크톱/태블릿/모바일 레이아웃 확인
- 문서 전체 가로 스크롤 없음
- 주요 모달 열기/취소/확인 흐름 검증
- `git diff --check` 통과
- 프로덕션 빌드 통과
- 관련 테스트 통과
- 콘솔에 신규 오류 없음
- 커밋·푸시·배포 요청이 있을 경우 라이브 배포 성공 확인

---

이 문서를 먼저 읽은 뒤 `git status`, 최신 커밋, 라이브 콘솔 상태를 다시 확인하고 작업을 시작할 것.

## 14. GPT-6 검수에서 추가 확인한 구현 간극

| 우선순위 | 항목·근거 | 현재 확인 내용 | 다음 작업·수용 기준 |
|---|---|---|---|
| 높음 | 권한: `worker/index.ts` API 분기 | 확인한 분기에서 사용자·역할 검사가 없음. 호스팅 게이트의 실제 보호 범위는 별도 검증 대상 | 허용되지 않은 사용자 요청이 상담·파일·설정 API에서 거부되는지 확인. 공개 운영 여부는 배포 도구로 조회 |
| 높음 | 분석: `analyze`, `extractedRtpResult`, `sttTranscript` | 고정 점수·추천·대화 예시가 실분석처럼 표시될 수 있음 | 샘플 표시 또는 실연동 구현. 서로 다른 원본 두 개로 결과 차이와 근거 검증 |
| 높음 | 공유: `shareReport` | 학생명 파라미터 URL 생성만 확인; 결과별 조회 경로 없음 | 고유 결과 ID·외부 읽기 전용 화면·접근 범위 구현. 별도 브라우저에서 해당 결과만 열리는지 검증 |
| 높음 | STEP 2 직접 업로드 콜백 | RTP는 파일명과 Object URL, 음성은 파일명 중심. 관리 화면의 POST 업로드와 다름 | 서버 업로드·고유 파일 ID 연결로 통일. 재접속·다른 기기에서도 원본 조회 |
| 높음 | 전송 시각: `deliveryCompletedAt` | 프런트 payload에만 존재; 상담 GET/POST/PUT·스키마에 대응 필드 없음 | DB 마이그레이션과 API 저장/반환 추가 후 재접속 시 시각 유지 |
| 중간 | WAV 저장: `saveRecording` → `upload` | 업로드 성공 전에 pending 녹음을 지우고 모달 닫음. upload는 실패를 내부 처리 | 업로드 실패에도 녹음 Blob을 유지하여 재녹음 없이 재시도 |
| 중간 | 파일 삭제: `remove` | DELETE 응답의 ok를 검사하지 않고 삭제 성공 안내 | 404/500/오프라인에서 실패 안내, 성공일 때만 성공 처리 |
| 중간 | 음성 업로드 | UI는 audio/* 선택 가능, 서버는 audio/mpeg 미지원 | MP3 지원 의도 확정 후 서버 허용·검증 또는 UI 허용 목록 정정 |
| 중간 | 저장 실패·상태 전환 | 자동 저장 오류를 숨기는 경로와 낙관적 완료 전환 존재 | 저장 중/실패/재시도 노출, 완료 클릭 후 즉시 새로고침 테스트 |

여기서 ‘소스 확인’은 해당 구현을 읽었다는 의미다. API 공격 테스트나 실제 데이터 손실 재현을 수행한 것은 아니다.

### 삭제와 데이터 관계

- 상담 DELETE는 `deleted_at`을 설정하는 논리 삭제다.
- 파일 DELETE는 **R2 원본을 실제 삭제한 후** 첨부파일 행에 `deleted_at`을 설정한다. 파일 복구가 가능하다고 가정하지 말 것.
- 상담의 RTP·음성 선택이 파일명 문자열 중심이므로 동명 파일, 이름 변경, 참조 중인 파일 삭제를 별도로 검사한다.
- 상태 이력은 존재하지만 실행자 문자열이 `담당자`/`시스템` 등으로 기록되는 경로가 있다. 실제 인증 사용자 감사 로그로 간주하지 않는다.

## 15. 검증·개발 재개 체크리스트

1. 본 문서와 기획서를 읽고 실제 현재 브랜치·변경분 확인. 문서상의 배포 버전은 마지막 성공 기록이다.
2. PowerShell에서 개발 서버를 실행할 때 package.json의 POSIX 환경변수 문법 문제를 피한다.
3. 기존 `tests/rendered-html.test.mjs`는 SSR/일부 소스·모델 검사이며 브라우저 E2E나 권한 검증을 대체하지 않는다.
4. 테스트용 자료와 상담으로 예약 생성부터 완료·재수정·전송·등록 후속관리까지 검증한다.
5. 자동 저장 뒤 새로고침, 즉시 화면 전환, 오프라인, 서버 오류, 중복 클릭을 포함한다.
6. 실제 음성 재생 완료, WAV 변환 후 업로드·재다운로드, PDF 전체 출력 및 외부 공유를 확인한다.
7. 모바일 320/396/560px, 중간 폭 600/700/900px, 데스크톱에서 경계 레이아웃과 긴 문자열을 검사한다. 과거 확인은 396px·1031px 등 제한된 크기였다.
8. 기능을 수정한 경우에만 관련 테스트와 빌드 수행 후 요청 범위에 따라 배포한다. 문서 검수 자체로 앱 수정·배포를 수행하지 않는다.

## 16. 검수 변경 이력

- v1.0: 작업 이력과 브라우저 QA를 바탕으로 최초 인수인계 작성, `566f21d`로 GitHub 반영.
- v2.0: 현재 소스와 기획서를 대조. WAV 변환, 작성 완료 후 이동, MP3 허용 여부 정정. AI/STT 샘플, 공유 조회 경로, 전송 시각 영속성, API 권한 검사, 업로드·삭제 실패 처리 추가. QA의 관찰 범위와 원인 가설을 명시. 최신 소스 커밋과 문서 커밋을 구분.
