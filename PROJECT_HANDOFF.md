# DIVE 상담 리포트 — LLM 인수인계 문서

> 최종 정리일: 2026-09-22 (Asia/Seoul)  
> 다음 담당: Claude 또는 후속 LLM  
> 목적: 지금까지 구현·수정·배포한 내용을 다시 탐색하지 않고 바로 이어서 작업할 수 있도록 정리

## 1. 프로젝트 현황

- 서비스명: **DIVE 상담 리포트**
- 용도: 학생 상담 예약, 자료 등록, RTP 결과 분석, 상담 결과 작성, 학부모 공유 및 등록 여부 추적
- 로컬 프로젝트: `C:\Users\black\OneDrive\문서\ChatGPT\dive project`
- GitHub: `git@github.com:blackol13/dive-consultation-report.git`
- 현재 작업 브랜치: `codex/consultation-workflow`
- GitHub `main`에 반영된 최신 커밋: `19f018a feat: refine responsive consultation interface`
- 라이브 URL: <https://dive-consultation-report.jaehwa0717.chatgpt.site/>
- 마지막 배포: Sites 버전 28, 커밋 전체 SHA `19f018aef84c99b862dbf30c3e5bd410cb39f2c1`
- Sites 프로젝트 ID: `.openai/hosting.json`에 저장되어 있음
- 영속 저장소:
  - D1 바인딩: `DB`
  - R2 바인딩: `FILES`

보안상 배포용 임시 토큰이나 쓰기 자격 증명은 이 문서에 기록하지 않는다. 다음 배포 때 Sites 도구를 통해 새 자격 증명을 발급받아야 한다.

## 2. 기술 스택과 실행 방법

- React 19
- TypeScript
- Vinext / Vite
- Cloudflare Workers 호환 빌드
- Drizzle ORM + D1
- R2 파일 저장
- Node.js 요구 버전: `>=22.13.0`

주요 명령:

```powershell
# 개발 서버
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 + 렌더링 테스트
npm test

# 정적 검사
npm run lint

# DB 마이그레이션 생성
npm run db:generate
```

이 컴퓨터에는 프로젝트 내부 Node 런타임이 있다. 시스템 Node가 맞지 않을 경우:

```powershell
$env:PATH=(Resolve-Path '.\.runtime\node-v22.23.2-win-x64').Path+';'+$env:PATH
$env:WRANGLER_LOG_PATH='.wrangler/wrangler-build.log'
npx vinext build
```

## 3. 핵심 파일 구조

| 파일/폴더 | 역할 |
|---|---|
| `app/page.tsx` | 전체 SPA 화면, 상태, 상담 워크플로우, 파일 관리 UI의 대부분 |
| `app/globals.css` | 데스크톱·태블릿·모바일 적응형 스타일 전체 |
| `app/layout.tsx` | 앱 레이아웃과 메타데이터 |
| `app/chatgpt-auth.ts` | 호스팅 인증 관련 코드 |
| `worker/index.ts` | `/api/consultations`, `/api/files`, `/api/settings` 등 Worker API |
| `db/schema.ts` | D1 스키마 |
| `db/runtime.ts`, `db/index.ts` | DB 연결 및 데이터 접근 |
| `db/migrations/` | 운영 스키마, 자료 등록 플래그, 설정 마이그레이션 |
| `tests/rendered-html.test.mjs` | 렌더링 결과 테스트 |
| `.openai/hosting.json` | Sites 프로젝트 ID와 D1/R2 논리 바인딩 |
| `public/dive-logo.png` | 브랜드 로고 |

`app/page.tsx`가 매우 큰 단일 파일이다. 기능 변경 시 상태와 뷰가 서로 강하게 연결되어 있으므로 함부로 일부만 떼어내지 말 것. 장기적으로는 화면별 컴포넌트와 훅으로 분리하는 리팩터링이 필요하지만, 현재는 동작 보존이 우선이다.

## 4. 현재 사용자 흐름

### STEP 1 — 학생 정보

- 학생 이름
- 학교 구분: 초등학교/중학교
- 학교명
- 학년: 학교 구분 선택 후 활성화
- 학부모 휴대번호
- 상담 예약 날짜·시간
- 담당자 메모
- 필수 정보가 채워져야 `상담 예약` 버튼 활성화
- 최종 등록 전 확인 팝업 표시
- 등록 완료 시 상담 기록의 `상담 대기` 필터로 이동

### STEP 2 — 자료 등록

- RTP PDF 업로드 또는 기존 등록 파일 선택
- 상담 음성 업로드 또는 기존 등록 파일 선택
- 각 자료는 `미등록` 선택 가능
- 기존 등록 파일은 `/api/files`에서 실시간 로드
- 샘플 파일은 워크플로우 선택 목록에서 제외
- 학생 정보 카드 하단에 담당자 메모 표시
- 학생 정보 수정 모달 제공
- `상담 기록 목록` 복귀 버튼 제공
- 자료 등록 완료 후 STEP 3 이동

### STEP 3 — 결과 작성

- RTP 결과 점수/분석 결과 표시
- 레벨 테스트 원장 리뷰
- STT 상담 내용 요약
- 상담 대화 내역 확인 모달
- 학생 레벨과 DIVE 반 배정
- 원장 코멘트
- 학생 정보와 담당자 메모 표시
- 학생 정보 수정 모달 제공
- `상담 기록 목록` 복귀 버튼 제공
- 작성 완료 시 확인 흐름 후 상태를 `작성 완료`로 변경하고 STEP 4로 이동

### STEP 4 — 결과 공유

- 링크 공유
- PDF 열기/인쇄
- 상담 후 팔로업:
  - 등록 여부: 미확인/등록 완료/미등록
  - 등록일
  - 기타 의견
- 상단에 `상담 기록 목록` 복귀 버튼
- 하단의 기존 `상담 기록으로 →` 버튼은 삭제됨
- `전송 완료` 버튼은 하단 워크플로우 액션 위치로 이동됨
- 전송 완료 클릭 시 확인 팝업
- 확인 시 상태를 `결과 전송 완료`로 저장하고 해당 필터가 활성화된 상담 기록으로 이동

## 5. 화면별 구현 내용

### 홈

- 오늘 날짜 표시
- 상담 음성 바로 녹음
- 신규 상담 등록
- 첨부파일 관리
- 오늘 상담 예정 수치
- 상담 결과 작성 대기 수치
- `오늘 상담 예정` 카드는 파란색 2px 테두리와 파란 수치로 강조
- `전체 상담 대기 보기 →`는 모바일에서 카드 우측 상단 배치

### 상담 기록

- 상태 필터:
  - 상담 대기
  - 상담 작성 중
  - 결과 작성 완료
  - 결과 전송 완료
- 검색: 학생명 또는 학교명
- 워크플로우에서 목록으로 돌아오면 기존 필터 유지
- 일정 태그 정렬 우선순위:
  1. Today
  2. Upcoming
  3. Past
  4. 같은 그룹에서는 최신 날짜·시간 우선
- Today/Upcoming/Past 태그는 `상담 대기`, `상담 작성 중`에서만 표시
- `작성 완료`, `결과 전송 완료`에서는 일정 태그 숨김
- `결과 전송 완료`에서만 등록 여부 표시
- 모바일 카드에서는 학생 이니셜 원형 아이콘 숨김
- 모바일에서 학생명은 카드 상단 왼쪽 정렬
- 모바일 등록 여부는 학년과 같은 오른쪽 열에 정렬
- 태블릿/데스크톱 테이블은 컨테이너 폭에 맞춰 컬럼 리사이즈
- 테이블에 가로 스크롤이 생기지 않도록 고정 최소 너비 제거 및 말줄임 처리

### 첨부파일 관리

- 탭:
  - 레벨 테스트 PDF
  - 상담 음성 WAV/MP4
- 모바일 하단 내비게이션에서 첨부파일 선택 시 녹음 팝업이 자동으로 열리지 않음
- PDF 등록 버튼과 녹음 파일 등록 버튼은 모바일 카드 우측 상단
- 바로 녹음은 별도 버튼
- 브라우저 녹음 결과는 파일명 확인 후 저장
- 녹음 결과 형식은 브라우저 `MediaRecorder` 지원 형식에 따라 WebM/MP4 계열일 수 있음
- 기존 사용자가 요청한 WAV/MP 파일 업로드는 업로드 입력에서 허용
- 파일 행 모바일 배치:
  - 삭제: 행 우측 상단
  - 미리보기/재생: 파일명 시작선에 맞춘 좌측 하단
  - 다운로드: 미리보기 오른쪽
- 아이콘:
  - PDF 미리보기: 눈 아이콘
  - 음성 미리듣기: 재생 아이콘
  - 다운로드: 내려받기 SVG 아이콘
- 샘플 파일은 다운로드와 삭제 비활성화
- 업로드된 실제 파일만 STEP 2 기존 파일 선택 목록에 표시

### 학생 정보 수정 모달

- `변경사항 반영` 클릭 시 “수정된 내용을 반영하시겠습니까?” 확인 팝업
- 확인: 정보 저장 및 모달 종료
- 취소: 확인 팝업 닫기
- 날짜·시간·메모 포함 수정 가능

### 반응형/적응형 웹

- 데스크톱: 좌측 사이드바
- 모바일: 하단 고정 내비게이션
- 모바일 STEP 네비게이터는 4단계 압축 표시
- STEP 3 학생 정보는 모바일에서 2×2 그리드
- STEP 4 목록 복귀 버튼은 네비게이터 아래 우측의 정상 크기 버튼
- 모바일 카드/파일 목록/모달/폼을 한 열로 재배치
- 최소 지원 폭은 320px
- QA한 크기:
  - 396×912 모바일
  - 1031×912 태블릿/소형 데스크톱
  - 기본 인앱 브라우저 약 910×698
- 위 크기에서 문서 및 상담 기록 테이블 가로 스크롤 없음 확인

## 6. 데이터와 API 동작

### 상담 데이터

- `GET /api/consultations`: 상담 목록 로드
- `POST /api/consultations`: 신규 상담 생성
- `PUT /api/consultations`: 상담 자동 저장 및 상태 업데이트
- `DELETE /api/consultations?id=...`: 상담 기록 삭제
- 상담 편집 중 주요 상태는 350ms 디바운스로 자동 저장

상담 상태 값:

```text
상담 대기
상담 작성 중
작성 완료
결과 전송 완료
```

### 파일

- `GET /api/files`: 파일 목록
- `POST /api/files`: R2 업로드
- `GET /api/files?id=...`: 다운로드/미리보기
- `DELETE /api/files?id=...`: 삭제
- 카테고리:
  - `rtp`
  - `consultation`

### 설정

- `GET /api/settings`: DIVE 반 시간표 로드
- `PUT /api/settings`: DIVE 반 시간표 저장

## 7. 최근 커밋 흐름

```text
19f018a feat: refine responsive consultation interface
68293f1 feat: adapt layouts and sync registered files
9d310b7 feat: refine consultation workflow and recording
af7723e feat: refine consultation workflow and reporting
b65903d feat: refine consultation history views
6485e5f feat: classify waiting consultations by schedule
70b4356 feat: default consultation history to waiting
8db36bf copy: clarify dashboard consultation metrics
```

`19f018a`에는 최종 반응형 조정, 파일 버튼 배치, 홈 강조 카드, 상담 기록 컬럼 리사이즈 등이 포함되어 GitHub `main`과 라이브 버전 28에 배포되어 있다.

## 8. 2026-09-22 라이브 기능 QA 결과

실제 라이브 사이트를 인앱 브라우저로 점검했다. 운영 데이터 변경을 피하기 위해 최종 저장/삭제/전송 확인은 실행하지 않았다.

### 통과

- 홈 화면 주요 메뉴 이동
- 신규 상담 등록 화면 진입
- 필수 입력 전 상담 예약 버튼 비활성화
- 학교 구분 선택 후 학년 활성화
- 상담 기록 상태별 필터
- 학생명/학교명 검색 및 결과 없음 상태
- 작성 완료/결과 전송 완료에서 일정 태그 숨김
- 기존 상담 재진입
- STEP 2·3·4 상태 표시
- 목록 복귀 후 필터 유지
- 학생 정보 수정 모달
- 수정 반영 확인 팝업과 취소
- 전송 완료 확인 팝업과 취소
- PDF/상담 음성 탭 전환
- 첨부파일 탭 진입 시 녹음 팝업 자동 미표시
- 실제 등록 음성 파일 목록 로드
- 바로 녹음 준비 팝업과 취소
- 음성 미리듣기 팝업 진입
- 모바일 학생 이니셜 숨김
- 모바일 파일 액션 정렬
- 모바일/태블릿 가로 스크롤 없음

### 발견된 결함 1 — React hydration 오류

라이브 최초 로딩 시 콘솔에 아래 오류가 1회 발생했다.

```text
Minified React error #418
```

화면 동작은 가능하지만 서버 렌더링 텍스트와 클라이언트 첫 렌더링 텍스트가 일치하지 않는 문제다. 가장 유력한 위치:

```tsx
const todayLabel = new Intl.DateTimeFormat("ko-KR", {
  year: "numeric",
  month: "long",
  day: "numeric",
  weekday: "long",
}).format(new Date());
```

`Dashboard` 렌더링 중 `new Date()`를 직접 사용하고 있으며 서버/클라이언트 시간대 또는 렌더 시점이 다르면 hydration mismatch가 발생할 수 있다. `today()` 함수도 로컬 시간에 의존한다.

권장 해결:

1. 서버와 클라이언트가 같은 `Asia/Seoul` 기준 날짜 문자열을 사용하도록 공통 함수화한다.
2. 또는 마운트 이후 날짜를 설정하고 최초 SSR에는 안정적인 placeholder를 사용한다.
3. 수정 후 프로덕션 빌드 및 라이브 콘솔 오류 재검증.

### 개선 필요 1 — 빈 결과 작성 완료 가능

STEP 3의 세 textarea가 모두 비어 있어도 `작성 완료 →` 버튼이 활성화된다. 빈 결과지를 실수로 완료 처리할 수 있다.

권장 해결:

- 필수 필드 정의 후 `disabled` 조건 추가
- 또는 완료 클릭 시 누락 항목을 설명하는 확인/검증 메시지 표시
- RTP 미등록과 음성 미등록이 허용되므로 필수 조건을 자료 존재 여부가 아닌 결과 텍스트/배정 값 중심으로 설계할 것

### 데이터 부족으로 미검증

- QA 당시 `결과 전송 완료` 기록이 0건이어서 상담 기록 카드의 등록 여부 표시를 실제 운영 데이터로 검증하지 못했다.
- 실제 마이크 권한 허용과 녹음 저장은 실행하지 않았다.
- 실제 파일 업로드·다운로드·삭제는 운영 데이터 보호를 위해 실행하지 않았다.
- 상담 신규 생성, 작성 완료 확정, 전송 완료 확정도 실행하지 않았다.

## 9. 다음 담당자가 우선 처리할 작업

우선순위 순서:

1. **React #418 hydration 오류 수정**
2. **STEP 3 완료 조건 정의 및 검증 추가**
3. 로컬에서 `npm test`, `npm run lint`, 프로덕션 빌드 수행
4. 테스트 전용 상담을 생성해 전체 E2E 수행:
   - 상담 생성
   - PDF/음성 업로드
   - 자료 선택
   - 결과 작성
   - 작성 완료
   - 전송 완료
   - 등록 여부 업데이트
   - 상담 기록 정렬/필터 검증
   - 테스트 데이터 삭제
5. 실제 모바일 Safari/Chrome에서 마이크 녹음 및 파일 포맷 확인
6. 단일 `app/page.tsx`를 기능 단위로 점진적 분리

## 10. 다음 작업 시 주의사항

- 기존 사용자 데이터가 라이브 D1/R2에 있으므로 QA 중 임의 삭제하지 말 것.
- 샘플 파일과 실제 업로드 파일의 동작이 다르다. 샘플은 삭제/다운로드/워크플로우 선택이 제한된다.
- 상담 기록에서 카드 클릭은 기존 상담을 재개한다. 테스트 중 자동 저장이 발생할 수 있다.
- 학생 정보 수정 모달에서 확인까지 누르면 실제 데이터가 변경된다.
- `작성 완료`, `전송 완료` 확인은 상태와 필터를 변경한다.
- 브라우저 마이크 권한은 사용자의 명시적 동의 없이 허용하지 말 것.
- `globals.css` 후반부에 반응형 override가 많이 누적되어 있다. 기존 규칙보다 파일 뒤쪽 규칙이 우선하므로 변경 시 cascade를 확인할 것.
- 모바일 기준 breakpoint가 `560px`, `600px`, `700px`, `900px` 등으로 나뉘어 있다.
- 테이블 가로 스크롤 제거 규칙은 `min-width:561px` 이상 상담 기록 컬럼에 별도로 적용되어 있다.
- 작업 전 `git status --short`로 사용자 변경과 임시 파일을 구분할 것.

## 11. 현재 Git 작업 트리 주의

인수인계 문서 생성 직전, 추적되지 않은 파일은 다음과 같았다.

```text
.preview-err.log
.preview-out.log
site-package-19f018a.tar.gz
site-package-68293f1.tar.gz
site-package-9d310b7.tar.gz
site-package-af7723e.tar.gz
site-package-b65903d.tar.gz
```

이 파일들은 개발 서버 로그와 이전 Sites 배포 패키지다. 소스 커밋에 포함하지 않았다. 삭제가 필요하면 프로젝트 루트 내부의 정확한 경로인지 확인한 뒤 처리할 것. 이 문서 `PROJECT_HANDOFF.md` 자체는 새 파일이므로 다음 커밋에 포함 여부를 사용자와 결정하면 된다.

## 12. 배포 절차

이 프로젝트에는 `.openai/hosting.json`이 있으므로 **Sites 빌드/호스팅 절차**를 따라야 한다.

일반 순서:

1. 변경사항 검증
2. `npm run build`
3. 소스 커밋
4. GitHub `main` 푸시
5. Sites 소스 저장소용 쓰기 자격 증명 발급
6. 같은 커밋을 Sites 저장소 `main`에 푸시
7. Sites 호스팅 스킬의 `package-site.sh`로 `dist` 배포 아카이브 생성
8. 정확한 커밋 SHA로 Sites 버전 저장
9. 사이트 접근 정책 확인
10. 저장된 버전을 프로덕션 배포
11. 배포 상태가 `succeeded`가 될 때까지 확인
12. 라이브 URL 재확인

배포 아카이브는 소스 트리가 아니라 성공한 빌드의 `dist`를 포함해야 한다. 기존 아카이브를 재사용하지 말고 현재 커밋과 일치하는 새 아카이브를 만든다.

## 13. 완료 기준

후속 변경은 아래를 모두 충족해야 완료로 간주한다.

- 요청 기능 구현
- 데스크톱/태블릿/모바일 레이아웃 확인
- 문서 전체 가로 스크롤 없음
- 주요 모달 열기/취소/확인 흐름 검증
- `git diff --check` 통과
- 프로덕션 빌드 통과
- 관련 테스트 통과
- 콘솔에 신규 오류 없음
- 커밋·푸시·배포 요청이 있을 경우 라이브 배포 성공 확인

---

이 문서를 먼저 읽은 뒤 `git status`, 최신 커밋, 라이브 콘솔 상태를 다시 확인하고 작업을 시작할 것.
